# ── Ants frontend environment ───────────────────────────────────────────────
# Copy to .env.local for local dev, or set in the Vercel dashboard.

# Where the Ants backend lives. Local default; in production point this at
# your deployed backend, e.g. https://ants-backend.up.railway.app
NEXT_PUBLIC_API_URL=http://localhost:8000
